interface:
  display_name: "Use Gesture"
  short_description: "Repo-specific patterns and workflows for use-gesture"
  default_prompt: "Use the use-gesture repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true